<header class="hero">
  <p class="eyebrow">Interactive LP</p>
  <h1>Feasible set &amp; simplex</h1>
  <p class="lede">
    Write an objective (<code>maximize</code> or <code>minimize</code>), a <code>subject to</code> line, then one
    constraint per line (commas between terms are fine). <strong>Analyze</strong> to solve, see the feasible region,
    follow the graphical tutor when it applies, and step through a simplex tableau when your model supports it.
    <strong>Print worksheet</strong> gives a clean page for notes or class.
  </p>
</header>

<style>
  .hero {
    margin-bottom: 1.75rem;
    max-width: 52rem;
  }
  .eyebrow {
    margin: 0 0 0.35rem;
    font-size: 0.72rem;
    font-weight: 600;
    letter-spacing: 0.14em;
    text-transform: uppercase;
    color: var(--color-text-muted);
  }
  h1 {
    font-family: var(--font-serif);
    font-weight: 600;
    font-size: clamp(1.85rem, 3.6vw, 2.35rem);
    line-height: 1.12;
    letter-spacing: -0.02em;
    margin: 0 0 0.65rem;
    color: var(--color-text);
  }
  .lede {
    margin: 0;
    font-size: 1.02rem;
    line-height: 1.6;
    color: var(--color-text-muted);
    max-width: 48rem;
  }
</style>
